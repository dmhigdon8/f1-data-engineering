name: 'f1'
version: '0.1.0'
config-version: 2

profile: 'f1'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  f1:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
